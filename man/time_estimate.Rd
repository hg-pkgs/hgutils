% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_estimate.R
\name{time_estimate}
\alias{time_estimate}
\alias{time_estimate.survRes}
\alias{time_estimate.survfit}
\alias{time_estimate.list}
\alias{time_estimate.coxph}
\title{Survival time estimation}
\usage{
time_estimate(fit, survival, ...)

\method{time_estimate}{survRes}(fit, survival = 0.5, ...)

\method{time_estimate}{survfit}(fit, survival = 0.5, ...)

\method{time_estimate}{list}(fit, survival = 0.5, ...)

\method{time_estimate}{coxph}(fit, survival = 0.5, newdata = NULL, ...)
}
\arguments{
\item{fit}{A survival fit object}

\item{survival}{the survival probability for which the timepoint is estimated.}

\item{...}{unused}

\item{newdata}{A dataframe containing predictors for which predictors are desired. See \code{\link[survival]{survfit.coxph}}.}
}
\value{
A named list or matrix with elements surv (estimate), lower and upper (confidence interval). The attribute 'survival' is added to the
result and set to the argument survival probability
}
\description{
Get estimate of timepoints for a given survival probability.
}
\section{Methods (by class)}{
\itemize{
\item \code{survRes}: for survRes objects

\item \code{survfit}: for \code{\link[survival]{survfit}} objects.

\item \code{list}: for \code{\link[rms]{survest.cph}} objects.

\item \code{coxph}: A \code{\link[survival]{coxph}} object.
}}

\examples{
library(rms)
fit = cph(Surv(time=time, event=status) ~ age + sex, data=lung,
  x=TRUE, y=TRUE, surv=TRUE)
sFit = survfit(fit)
sFit2 = survfit(fit, newdata=lung[1:20,])

time_estimate(sFit) #get median survival for all data points in the dataset.
time_estimate(sFit2) #get median survival for patients 1-20 seperately
}
